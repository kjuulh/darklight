subscription SubscribeDownload($downloadId: ID!) {
    getDownload(downloadId: $downloadId) {
        download {
            id
            link
            file
            percentage
        }
    }
}
